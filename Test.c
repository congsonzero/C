#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   float a=5.0,b=2.0,i=0;
   printf("Gia tri a=%d\n",a);
   printf("Gia tri b=%d",b);
   if (i==0)
   {printf("\ni is True");};
   printf("\nket qua a/b %2f ",a/b);
   return 0;
}